本项目适合想学`TUI`的初学者学习📖！

# 项目框架
本项目先获取了`text`下的**文件列表**，然后取**文件名**显示，

然后用换行分割句子。

## 输入规则
1. 如果是正确的字符，则下一个字符，如果是该句末尾就下一句，如果是文章末尾就结束。
2. 如果1不成立并且输入的是英文能有的字符(`a`~`z`,`A`~`Z`,`.!?'`)那么就错误字符增加
3. 如果2不成立且输入`ESC`就**退出**
